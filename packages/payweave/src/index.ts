/**
 * Payweave — one SDK, every provider, woven together.
 *
 * Public facade. The real surface (PaymentSDK, createPaystack, createFlutterwave)
 * lands in a later wave (see docs/backlog.md — PW-101+). For now this exports a
 * version placeholder so the package builds and publishes as `payweave@0.0.0`.
 */
export const VERSION = "0.0.0";
