// Fixture (test/cli/init.test.ts): a filesystem-marker-only Next.js project —
// no package.json at all yet (e.g. `next.config.mjs` committed before
// `next` lands in dependencies). detectFramework must still say "next".
export default {};
