// PW-1002 fixture: no default export and no named `payweave` export at all —
// the "wrong export shape" message's "not found" sub-case (distinct from
// wrong-export/, where a default DOES exist but isn't a client).
export const somethingElse = 1;
